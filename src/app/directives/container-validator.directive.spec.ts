import { ContainerValidatorDirective } from './container-validator.directive';

describe('ContainerValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new ContainerValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
